const productsStyles = (theme) => ({
    submitButton: {
        width: '100%',
        display: 'flex',
        justifyContent: 'flex-end',
        paddingBottom: '10px'
    },
});

export default productsStyles;